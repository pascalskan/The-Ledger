STOP ALL CODE CHANGES UNTIL YOU READ THIS FULLY.

You are now implementing researched, real-world object page designs for “The Ledger”.
Do not guess. Follow this specification exactly.

PART 1 — Object Detail Pages (Full Pages, No Popups)

Implement dedicated detail pages for:
- Jobs (/jobs/:id)
- Clients (/clients/:id)
- Workers (/workers/:id)
- Equipment (/equipment/:id)
- Invoices (/invoices/:id)

Each page must:
- Load correct data by ID
- Show a loading state
- Handle not-found safely
- Be a full-width page (no modals)
- Link seamlessly to related objects

Use real business expectations:
- Client pages show jobs, invoices, documents, financial summary
- Worker pages show availability, current/planned jobs, history
- Equipment pages show usage timeline, service history, maintenance issues
- Invoice pages must resemble real invoices (header, line items, totals, bank details)
- Job pages are the central hub linking all other objects

Clicking any linked object must navigate to its own detail page.

PART 2 — Schedule Page

Create a Schedule page with:
- Month view (calendar-style)
- Week view (horizontal time, vertical lanes)

Allow toggles for:
- Planned / Active / Completed
- View by Jobs / Workers / Equipment / Clients

Clicking any item navigates to its detail page.
No popups. No forced editing.

PART 3 — Map Behaviour

Fix map behaviour to follow these rules:
- Default: markers only, colour-coded by job status
- Clicking a marker shows a brief summary
- Clicking “View Job” navigates to /jobs/:id
- No job-specific overlays unless a job is explicitly selected
- Map must never leak job-detail state globally

PART 4 — Invoices

Invoices must:
- Match real-world invoice structure
- Be linked to jobs and clients
- Be printable and sendable
- Include company bank/payment details
- Be editable before sending, locked after payment

PART 5 — NO NEW FEATURES

Do NOT invent features.
Do NOT change existing behaviour unless required by this spec.
Do NOT modify unrelated code.

Once implementation is complete:
- Report what was implemented
- Confirm all navigation paths work
- Wait for approval before continuing
