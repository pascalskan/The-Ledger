FOLLOW-UP CHANGE REQUEST (The Ledger — Example Business Account, Preserve CEO Role, Demo Environment)

Continue from the CURRENT codebase. Do NOT delete or replace any existing roles or users. This is a data-structure and seeding change, not a role removal.

Goal:
Introduce a fully populated “Example Business” account that demonstrates what a real, functional business looks like inside The Ledger. This account is used purely for demonstration, proof of concept, and onboarding — while all real accounts and roles (including CEO) continue to exist normally.

A) Preserve the CEO Role (No Deletion)
1) Do NOT delete the CEO role.
2) Do NOT downgrade or alter the CEO role’s permissions.
3) The existing CEO role remains available for real businesses and users.
4) All existing accounts and roles must continue to exist unchanged.

B) Create a Separate “Example Business” Account (Demo Tenant)
1) Create a new Company/Tenant:
   - name: “Example Business” (or “Ledger Demo Company” if you prefer)
   - flagged internally as isDemo = true (boolean or metadata field)
2) This demo company is completely separate from all other companies.
3) Data from the previously filled CEO account should be REIMPLEMENTED under this new Example Business account:
   - Jobs
   - Clients
   - Workers
   - Equipment/Assets
   - Assignments
   - Invoices
   - Documents
   - Schedule data
4) Do NOT migrate or move real user data between companies.
   - Instead, reseed equivalent demo data under the Example Business tenant.

C) Example Business Purpose & Behaviour
The Example Business must:
- Look busy and realistic
- Demonstrate best-practice use of The Ledger
- Clearly show:
  - Multiple active jobs
  - Overlapping schedules
  - Assigned workers and equipment
  - Invoices linked to jobs and clients
  - Documents attached to jobs
  - Map view filled with coloured job markers
- Act as a “what your business could look like” showcase

D) Example Business Users & Roles
1) Create demo users within the Example Business tenant only:
   - Example CEO / Owner (full access)
   - Example Project Manager
   - Example Worker
2) These users:
   - Are clearly labelled as demo users (e.g., “Demo CEO”)
   - Have realistic permissions per role
3) Output demo credentials clearly in console and README:
   - Email + password for each demo user
4) These demo users must NOT interfere with real users or real businesses.

E) UI Clarity (Important)
1) When logged into the Example Business:
   - Display a subtle banner or badge:
     “Example Business — Demo Data”
2) Make it obvious (but not intrusive) that:
   - This is a demonstration environment
   - Data changes here do not affect real businesses
3) The UI behaviour is identical to a real business (no feature limitations).

F) Data Integrity & Isolation
1) Tenancy rules remain strict:
   - Example Business data is visible ONLY to Example Business users.
   - Real business users can never see demo data.
2) Audit logs must still function normally within the Example Business.
3) Invoices, documents, and schedules behave exactly as they do for real tenants.

G) Implementation Notes
- This is NOT a mock mode.
- This is a real tenant with seeded data.
- No hardcoded UI hacks.
- Treat the Example Business exactly like a normal customer internally, with a demo flag only for identification.

Deliverables:
- Example Business tenant exists and is populated
- CEO role remains intact and reusable
- All original accounts continue to exist
- Demo credentials clearly provided
- UI clearly indicates demo environment
- No regressions to auth, RBAC, or tenancy

Proceed to implement. Modify seed scripts, data models (if required), and UI where needed to support this cleanly.
