FOLLOW-UP FIX REQUEST (Object Detail Pages Broken + Map Behaviour Regression)

Continue from the CURRENT codebase. This is a FIX + REALIGNMENT task. Do NOT introduce new concepts yet.

A) Object detail pages are NOT working (critical)
Clicking into a specific object currently does NOT function correctly.

This applies to ALL core objects:
- Jobs
- Clients
- Equipment
- Workers

What MUST happen (as originally agreed):
1) List pages:
   - /jobs
   - /clients
   - /equipment
   - /workers
   These pages must ONLY show lists + create actions.
   No popups.
   No automatic detail rendering.
2) Detail pages:
   - /jobs/:id
   - /clients/:id
   - /equipment/:id
   - /workers/:id
   These routes must:
   - Load the correct object by ID
   - Handle loading + not-found states
   - Render a full-width, dedicated page (NOT blank)
   - Display real data for the selected object
3) Clicking a card in a list MUST:
   - Navigate to the correct :id route
   - Load and display that object’s data
4) Fix whatever is currently preventing this from working:
   - Routing
   - Data fetching
   - useParams / loader logic
   - Tenant filtering
   - Missing ID handling

This is non-negotiable — object detail pages must work reliably.

B) Ensure behaviour matches the original intent
Re-align with what we agreed earlier:
- Extra information (heatmaps, usage, tracking, related entities) is ONLY shown:
  - when viewing a specific object
  - on its dedicated :id page
- NEVER on list pages
- NEVER globally

C) Map regression — fix immediately
The map has gone wrong (as shown in the second image).

Correct behaviour:
1) Default map view:
   - Shows only job markers
   - No expanded panels
   - No job-specific overlays
   - Clean, readable map
2) Extra map information (routes, heatmaps, overlays, highlights):
   - MUST ONLY appear when:
     - viewing /jobs/:id
     - OR explicitly selecting a job
3) When NOT viewing a specific job:
   - Map must reset to default state
   - No selected-job artefacts
   - No weird overlays or UI clutter
4) Fix any state leakage where:
   - Selected job persists across pages
   - Map renders detail-mode UI globally

D) Explicit technical requirements
1) selectedJob (or equivalent state) must:
   - Be scoped to detail pages
   - Be reset on route change
2) Map component must:
   - Accept an optional jobId
   - Render detail overlays ONLY when jobId exists
3) List pages must never pass job-specific data into the map.

E) Validation checklist (you must test these)
- /jobs loads cleanly
- Clicking a job opens /jobs/:id and shows data
- /clients/:id loads client data
- /equipment/:id loads equipment data
- /workers/:id loads worker data
- Map is clean on list pages
- Map shows extra info ONLY on job detail pages
- Navigating back clears job-specific UI

Do NOT add new features yet.
Fix routing, state handling, and map behaviour so it works exactly as originally discussed.

Once complete, report what was fixed and confirm the validation checklist passes.
