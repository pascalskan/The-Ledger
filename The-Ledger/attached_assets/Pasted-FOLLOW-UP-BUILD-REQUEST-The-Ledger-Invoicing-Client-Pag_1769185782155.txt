FOLLOW-UP BUILD REQUEST (The Ledger — Invoicing + Client Pages + RBAC + Fix Editing + Simulations)

Continue from the CURRENT rebuilt Ledger codebase you generated (do NOT start a new repo). Implement the following changes end-to-end. 
Non-negotiable: every new button/flow must work fully (create/edit/delete/print/send), no placeholders.

A) Invoices + Clients (Job → Client relationship)
1) Add a “Client” entity and enforce that every Job belongs to exactly one Client.
   - Client fields: id, companyId, clientId (readable like CL-000001), name, email, phone, billingAddress, notes, createdAt, updatedAt.
2) Update Job schema to require clientId (FK).
3) Create a Client detail page that acts as the “hub” for all client information:
   - Client profile (contact + billing)
   - List of all Jobs for that client (with job status, dates, value, outstanding balance)
   - All Documents linked to that client’s jobs (file upload/download)
   - All Invoices for that client (list, filter, status)
4) Documents:
   - Each Job can have multiple documents.
   - Documents should be visible from Job detail AND from the Client page (aggregate view).
   - Support upload/download/delete with permissions and audit logging.
5) Invoices:
   - Add Invoice entity:
     - id, companyId, invoiceId (INV-000001), clientId, jobId (optional but recommended), issueDate, dueDate, status (draft/sent/paid/overdue/void),
       lineItems JSON (description, qty, unitPrice, taxRate), subtotal, taxTotal, total, notes, createdAt, updatedAt.
   - Invoice must be printable (generate a clean print-ready invoice view).
   - Add “Download PDF” (or print-to-PDF) and “Forward/Send to client” button.
     - If email sending is too complex now, implement “Forward” as:
       - generate a shareable PDF download link OR open the user’s email client with a prefilled email (mailto) AND attach instructions.
     - Prefer real email sending if you can implement with a provider (SendGrid/etc.) safely via env vars.
   - Show invoice history under each Client and optionally under each Job.
   - Auto-mark invoice as “overdue” when dueDate passes (can be computed dynamically or via scheduled job).

B) Fix Editing (Critical Bug)
Right now editing existing objects does not work reliably (e.g., “Job settings” edit page fails / doesn’t persist).
1) Audit every edit flow for Jobs, Workers, Equipment/Assets, Clients, Invoices:
   - Edit forms must load existing data correctly.
   - Save must persist changes.
   - UI must update immediately after save.
   - Deleting must work and remove from UI.
2) Ensure server-side validation and proper error messages.
3) Ensure all edits/deletes create AuditLog entries with before/after snapshots.

C) Multi-user Accounts + Role-based UI/Authority (RBAC)
1) Each Ledger user must have their own login. No shared accounts.
2) When a new user account is created by an admin, they must be assigned a Role that defines permissions.
3) Create at least 3 roles (you choose) with a sensible “hierarchy of needs” approach:
   Example roles:
   - CEO/Owner (full access)
   - Project Manager (jobs/workers/equipment + assignments + limited financial access)
   - Worker (read-only access to their assigned jobs + basic job details + documents permitted)
   You may adjust names but keep 3+ tiers.
4) The UI must change depending on role:
   - Sidebar nav shows only allowed pages.
   - Attempts to access restricted pages by URL must be blocked server-side.
   - Dashboard widgets must reflect what the user is allowed to see (e.g., Worker sees “My Jobs Today”, Manager sees “Upcoming Conflicts”, Owner sees “Revenue/Invoices/Overdue”).
5) Worker hierarchy:
   - Worker operational “role tag” (like Crew Lead) can remain a label, but Ledger User role must control permissions.
   - Ensure roles can be customised by a business admin (rename role, toggle permissions), but keep default roles preloaded.

D) Seed / Simulate Users & Provide Credentials
1) Add seed data for development that creates:
   - One Company
   - Three Users with different roles (Owner/CEO, Project Manager, Worker)
   - A few Clients, Jobs, Equipment, Assignments, and Invoices tied together realistically.
2) Print the credentials clearly in the console on seed completion AND write them in README:
   - Admin/Owner credentials (email/password)
   - Project Manager credentials
   - Worker credentials
Use safe demo passwords but ensure they work.

E) Simulate Critical Issues (Testing + Handling)
Simulate at least 2–3 critical real-world issues and ensure the system handles them gracefully with clear UI messages and logs:
Examples (pick at least three and implement):
1) Scheduling conflict:
   - Assign the same worker to overlapping jobs → should block or require override permission + log override.
2) Equipment double-booking:
   - Assign same equipment to two jobs at same time → should block or require override + log.
3) Invoice overdue:
   - Create invoice due yesterday → show as overdue on dashboards & client page.
4) Permission violation:
   - Worker tries to access Invoices page or another client’s data via URL → must be blocked + log attempt.
5) Editing failure regression:
   - Add a test ensuring edit flows persist.

Implementation Order (do this in order):
1) Database migrations: Client + Invoice + Job.clientId + doc relations
2) Fix edit flows across entities
3) Implement Client pages (hub)
4) Implement Invoice creation + printable view + download/send
5) RBAC enforcement + role-based UI
6) Seed data + credential output
7) Simulations + minimal automated tests for conflicts/RBAC/edit persistence

Deliverables:
- Working UI for Clients and Invoices with print/send capability
- Fully working edit/delete across all entities
- 3+ roles with role-based dashboard + navigation
- Seed users with credentials displayed
- Simulated critical issues handled + logged
- README updated with setup + demo logins

Proceed to implement now. Don’t just describe—write the code, migrations, and UI changes.
