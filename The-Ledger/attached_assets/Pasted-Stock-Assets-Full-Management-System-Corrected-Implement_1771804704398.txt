Stock & Assets Full Management System (Corrected Implementation)
🎯 OBJECTIVE

Rebuild the Stock & Assets page so that:

Stock and Assets are fully manageable from within this page.

Stock and Assets are separate systems (separate logic).

Adding stock updates quantity if item exists.

Adding an asset checks for duplicates and prompts user before proceeding.

Removing an asset requires confirmation.

Asset status management includes operational reasons (service, broken, etc.).

Worker reports support separate search bars for Stock and Assets.

Each Stock item, Asset, and Location has its own dedicated detail page.

System is production-ready and logically correct.

Do not patch the existing logic — refactor cleanly.

🧱 PART 1: DATA STRUCTURE (MANDATORY)

Ensure three separate entities:

StockItem
id
name
sku
category
quantity
unitCost
reorderLevel
locationId
createdAt
updatedAt
Asset
id
name
serialNumber (unique if applicable)
type
status
statusReason
locationId
purchaseDate
lastServiceDate
nextServiceDate
assignedToJobId (nullable)
createdAt
updatedAt
Location
id
name
type (warehouse | vehicle | site | storage)
description
createdAt

Stock and Asset must NOT share tables.

🧾 PART 2: STOCK MANAGEMENT LOGIC
➕ Add Stock Form

Create modal or page form:

Fields:

Name (required)

SKU (required)

Category

Quantity (required)

Unit Cost

Reorder Level

Location (dropdown)

On Submit Logic

When form is submitted:

Check if StockItem exists where:

name + SKU + locationId matches

If EXISTS:

Update quantity:
newQuantity = existing.quantity + submitted.quantity

Update unitCost if provided

Show success message:
“Existing item updated. Quantity adjusted.”

If DOES NOT EXIST:

Create new StockItem

Show success message:
“New stock item created.”

This must happen server-side (not just frontend).

🛠 PART 3: ASSET MANAGEMENT LOGIC
➕ Add Asset Form

Fields:

Name (required)

Serial Number (optional but unique if provided)

Type

Location

Purchase Date

Last Service Date

Next Service Date

On Submit Logic

Check if asset already exists:

If serialNumber exists → check by serialNumber

Else check by name + location

If EXISTS:

Show confirmation modal:
“This asset already exists. Are you sure you want to add another?”

Buttons:

Cancel

Confirm Add Anyway

Only create asset if user confirms.

❌ Removing an Asset

When user clicks Delete:

Show confirmation modal:

“Are you sure you want to remove this asset? This action cannot be undone.”

Buttons:

Cancel

Confirm Delete

Delete only after confirmation.

🔄 Asset Status Management

Assets must support structured statuses:

Allowed statuses:

Active

In Service

Needs Service

Broken

Under Repair

Retired

When changing status:

Require a reason field if:

Broken

Under Repair

Retired

Store:

status
statusReason
statusUpdatedAt

Create status update history log.

📋 PART 4: WORKER REPORT FIX

Currently:
Worker report search only shows assets.

This must be corrected.

In Worker Report form:

Add TWO separate searchable dropdowns:

1️⃣ Asset Search

Searchable select

Pull from Asset table

Only show Active assets

2️⃣ Stock Search

Searchable select

Pull from StockItem table

Show available quantity

Allow user to enter quantity used

When stock is submitted in report:

Deduct used quantity from StockItem.quantity

Log stock movement record

Ensure stock cannot go negative.

📦 PART 5: DEDICATED DETAIL PAGES

Each entity must be clickable.

📄 Stock Detail Page

Route:

/stock/:id

Show:

All item info

Quantity

Location

Movement history

Jobs where used

Reorder level

Edit button

Adjust quantity button

📄 Asset Detail Page

Route:

/assets/:id

Show:

All asset info

Current status

Status history

Service history

Assigned job

Location

Edit button

Change status button

📄 Location Detail Page

Route:

/locations/:id

Show:

All stock at location

All assets at location

Total stock value

Active asset count

Alerts at this location

📊 PART 6: STOCK MOVEMENT LOG

Create new entity:

StockMovement

id
stockItemId
quantityChange
reason (manual adjustment | worker report | transfer | reorder)
jobId (nullable)
createdAt

Every quantity change must create a StockMovement record.

🚨 PART 7: ALERT LOGIC

Low stock:
If quantity <= reorderLevel → flag as low stock.

Service alert:
If nextServiceDate <= today → flag as needs service.

Show alerts in:

Stock list

Asset list

Location page

Alerts tab

🎨 UI REQUIREMENTS

Use modals for Add forms

Use confirmation modals for deletes

Use searchable dropdown components

Display badges for asset status

Highlight low stock in red

Highlight broken/needs service in red

Fully responsive tables

🔐 VALIDATION RULES

No duplicate serial numbers

Stock quantity cannot be negative

Asset deletion must require confirmation

Status reason required for broken/retired

All writes must be backend validated

🧼 CLEAN ARCHITECTURE

Separate StockService and AssetService

No shared logic

All quantity updates handled server-side

Use proper REST routes:

POST /stock

PATCH /stock/:id

POST /assets

PATCH /assets/:id/status

DELETE /assets/:id

📦 FINAL EXPECTED RESULT

When complete:

Stock can be added and auto-updated if duplicate

Assets require duplicate confirmation

Assets require delete confirmation

Worker reports correctly separate stock and assets

Stock deducts correctly when used

Every item has a detail page

Locations show aggregated data

All logic validated server-side

If anything is unclear, refactor the existing implementation to meet these requirements cleanly and professionally.