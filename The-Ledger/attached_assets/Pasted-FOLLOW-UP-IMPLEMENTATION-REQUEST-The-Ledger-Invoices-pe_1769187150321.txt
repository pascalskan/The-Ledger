FOLLOW-UP IMPLEMENTATION REQUEST (The Ledger — Invoices per Client/Job, QA Pass, UI Fixes, Busy Demo Data, Sidebar Collapse, Role Manager Access, Job→Invoice Template)

Continue from the CURRENT codebase. Do NOT start over. Implement the following changes end-to-end and verify each one by actually running through the UI flows yourself (create/edit/delete, role access, logout redirect, etc.). No placeholders.

A) Invoices must be creatable for a Client (and optionally tied to a Job)
1) Ensure “Create Invoice” flow requires selecting a Client.
   - If invoice is created from a Job, auto-select that Job’s Client and link invoice.jobId + invoice.clientId.
   - If created from Client page, invoice.clientId is mandatory and jobId is optional.
2) Confirm all invoice CRUD works:
   - Manual create
   - Edit
   - Delete
   - View/print/download (template output ok)
   - Status updates (draft/sent/paid/overdue)
3) Add clear success/error toasts for every action, and ensure lists update immediately after changes.

B) Fix logout redirect (no refresh required)
1) When logging out, the app must immediately redirect to the login page without requiring a manual refresh.
2) Ensure client-side state is cleared and protected routes do not flash.

C) Busy proof-of-concept dataset (make it look “alive”)
1) Create seed/demo data that intentionally creates a “busy schedule”:
   - At least 10–20 Jobs across multiple dates (some overlapping)
   - At least 8–12 Clients
   - At least 10–20 Workers (with different operational role tags)
   - At least 15–30 Equipment/Assets
   - Multiple assignments per job (workers + equipment)
2) Ensure the Jobs board, dashboard widgets, and map/schedule views show an active, real operation:
   - “Today’s jobs”, “This week”, “Conflicts”, “Overdue invoices” etc.
3) The purpose is to clearly demonstrate ease of management and visibility.

D) “Manage Roles” access issue
1) I cannot access the Manage Roles page/button. Fix this.
2) Ensure:
   - The sidebar shows “Manage Roles” ONLY for users with permission (Owner/CEO/Admin).
   - The route is protected server-side and client-side.
   - Clicking it opens a working Roles & Permissions page (create role, edit permissions, assign roles to users).
3) Add a safe fallback: if user lacks permission, show “Not authorized” page (no blank screen).

E) Sidebar collapse/expand button
1) Add a left sidebar toggle button that collapses the menu completely to icon-only mode.
2) In collapsed mode:
   - Show icons only
   - Show tooltips on hover
3) Allow expand back to full labels.
4) Store preference in localStorage per user so it persists.

F) Job board workflow: Schedule → Invoice Template
1) When a schedule/assignment is created or a job is moved into a scheduled/active state, the UI must offer: “Create Invoice Template”.
2) Invoice Template requirements:
   - Must be created from the Job board or Job detail screen.
   - Must auto-link to that job AND its client.
   - Must pre-populate invoice line items based on job fields/assignments (template logic is fine):
     Example line items:
       - Labour (hours x rate) OR “Crew labour”
       - Equipment usage (flat rate or per-day)
       - Materials (optional manual line)
   - Must allow custom input/editing (especially price/rates and quantities).
3) Include “Payment / Bank Details” section on the invoice template:
   - Add Company Settings fields:
     - companyLegalName
     - bankName
     - accountName
     - accountNumber
     - sortCode (UK)
     - reference format (e.g., invoiceId)
   - When invoice template is generated, it includes these details automatically.
4) Sending to client:
   - When invoice status is changed to “Sent”, trigger a “Send to client” action.
   - If email provider is not configured, implement a working fallback:
     - Generate a downloadable PDF and show a “Copy email text” + “mailto:” prefilled draft to client email.
   - Ensure “Sent” state is logged in AuditLog.

G) Quality pass: verify ALL buttons/concepts work
1) Perform a QA pass on core flows and fix any broken routes or actions:
   - Create/edit/delete Worker
   - Create/edit/delete Job
   - Create/edit/delete Equipment
   - Assign/unassign Worker and Equipment
   - Create/edit/delete Client
   - Create/edit/delete Invoice
   - Role management actions (create role, assign role)
   - Map view loads jobs
   - Sidebar toggle works
   - Logout redirects correctly
2) Add a lightweight “QA checklist” page or README section listing each flow and confirming it works.

Implementation order:
1) Fix logout redirect
2) Fix Manage Roles access and route protection
3) Make invoice create require client, ensure full CRUD works
4) Implement Job→Invoice Template flow + bank details from Company Settings
5) Add sidebar collapse/expand
6) Expand seed data to create a busy schedule demo
7) Run QA pass and fix any broken actions; update README/QA checklist

Proceed to implement now. Don’t explain only—change code, UI, routes, and seed data so everything works end-to-end.
