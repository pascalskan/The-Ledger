Task: Fix inline client creation and role management logic

You are working on The Ledger web app.
Do not redesign UI. Fix logic, data flow, and missing functionality only.

1. CREATE JOB → INLINE CLIENT CREATION (CRITICAL)
Current problem

When creating a job, if the client does not exist, there is no way to create one inline.

Required behavior (exact)

In the Create New Job modal:

In the Client selection dropdown:

Add a visible option: “+ Create new client”

Selecting this option must:

Open a Create Client modal ON TOP of the Create Job modal

NOT navigate to the Clients page

NOT close or reset the Create Job modal

After client creation:

Save the new client

Automatically select it in the job’s Client field

Preserve all job form state (title, dates, costs, etc.)

This must be implemented as a nested modal, not routing.

If any routing to /clients exists for this flow, remove it.

2. WORKERS → MULTI-ROLE ASSIGNMENT (FIX DATA + UI)
Current problems

Workers cannot reliably add or modify roles

Only single roles are supported

UI does not reflect actual role assignments

Required changes

When creating or editing a worker:

Allow assigning multiple roles

Display all available roles

Use multi-select (checkboxes or equivalent)

Persist roles as an array (worker ↔ roles relationship)

On worker views:

Clearly show all assigned roles

Ensure changes save correctly

3. MANAGE ROLES — PERMISSIONS, NOT TEMPLATES
Current problems

Roles are created from templates

User Assignments do not update correctly

Permissions are unclear

Required role model (MANDATORY)

Roles must be:

Created by selecting permissions, NOT templates

A role = a named set of permissions

When creating a role:

User selects permissions (e.g.:

View jobs

Edit jobs

Manage workers

Assign roles

View audit log
)

Save role with:

Name

Description

Permission set

Manage Roles page updates

Add:

Search input to filter roles by name

Clear list of:

Role name

Description

Permissions included

User Assignments (FIX)

The User Assignments section must:

Reflect the actual roles assigned to users

Update immediately when roles are added/removed

No hardcoded role types

No template-based role assumptions

User ↔ Role ↔ Permission relationships must be consistent across:

Worker pages

Manage Roles

User Assignments

DONE CHECKLIST (MUST ALL PASS)

Create Job → Client dropdown includes “+ Create new client”

Creating a client does NOT navigate away

Job form state is preserved

Workers can have multiple roles

Roles are permission-based, not templates

User Assignments reflects real role data

Manage Roles has search and permission visibility

Important

If any existing implementation conflicts with the above, replace it.