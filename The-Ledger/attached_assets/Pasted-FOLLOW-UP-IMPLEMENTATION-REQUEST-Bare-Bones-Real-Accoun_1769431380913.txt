FOLLOW-UP IMPLEMENTATION REQUEST (Bare-Bones Real Accounts, Map Data Binding, Fix Editing & Broken Buttons)

Continue from the CURRENT codebase. Do NOT reset or rebuild. This is a corrective + behaviour-refinement pass.

A) Bare-bones experience for REAL (non-demo) accounts
1) All real/main accounts (any company where isDemo = false) must start with MINIMAL data:
   - No pre-filled jobs
   - No pre-filled clients
   - No pre-filled workers (except the logged-in user)
   - No pre-filled equipment
   - No invoices
2) These accounts should feel “empty but ready”:
   - Dashboard shows empty-state messaging (e.g. “No jobs yet”, “Create your first client”)
   - No fake/demo data appears in real accounts
3) The Example Business (demo tenant) remains fully populated and unchanged.
4) Ensure strict tenancy separation:
   - Demo data NEVER appears in real accounts
   - Real accounts NEVER see demo data

B) Fix Map: bind real job data (critical)
The map currently renders visually but does NOT display job data.
1) Fix the map so it:
   - Pulls jobs from the database
   - Displays ONLY jobs for the current tenant
2) Each job must appear as a marker:
   - Marker colour or icon reflects job status (planned / active / completed)
   - Marker location comes from job latitude/longitude OR geocoded address
3) Clicking a marker must:
   - Show job summary (jobId, title, client, status, dates)
   - Link to the Job detail page
4) Ensure the Example Business map looks busy and populated.
5) Ensure real accounts show either:
   - their own jobs, or
   - a clear “No jobs to display yet” state.

C) Enable editing of Clients (currently broken)
1) Ensure Client edit functionality works end-to-end:
   - Open Client edit form
   - Fields pre-fill correctly
   - Save persists changes
   - UI updates immediately
2) Enable Client delete where permitted (with confirmation).
3) Ensure all Client edits/deletes are logged in AuditLog.

D) Fix broken buttons and actions (mandatory QA pass)
Several buttons still exist but do not function.
1) Audit and fix ALL of the following:
   - Create Role button
   - Edit Role
   - Assign Role to User
   - Create / Edit / Delete Job
   - Create / Edit / Delete Client
   - Create / Edit / Delete Worker
   - Create / Edit / Delete Equipment
   - Create / Edit / Delete Invoice
2) Every button must:
   - Trigger a real action
   - Handle success and failure
   - Update the UI state immediately
3) Remove or disable any button that cannot be made functional.

E) Fix Job → Invoice Template button (layout + logic)
As shown in the UI image:
1) The “Create Invoice Template” button does not fit correctly in the Job view.
   - Fix layout so the button is visible, aligned, and accessible.
2) Fix logic so clicking the button:
   - Creates an invoice template
   - Auto-links it to the Job and its Client
   - Pre-fills line items based on job data (template logic acceptable)
3) After creation:
   - Redirect to Invoice edit/view page
   - Show success confirmation
4) Ensure this flow works in the Example Business AND in real accounts.

F) Final verification
1) Manually test (or simulate) the following:
   - Real account login → clean empty dashboard
   - Demo account login → busy populated dashboard
   - Map displays job markers correctly
   - Client edit saves correctly
   - Role creation works
   - Invoice template creation from Job works
2) Fix any regression discovered during testing.

Deliverables:
- Real accounts are bare-bones and clean
- Example Business remains fully populated
- Map displays real job data
- Client editing works
- All buttons function or are removed
- Invoice template button fits and works correctly

Proceed to implement now. Do not explain only — update code, UI, routes, and data handling so this works end-to-end.
